# 飲み会割り勘アプリの機能整理

## 目的
学年ごとの傾斜を反映した割り勘を計算し、飲み会の負担額を「きりの良い金額」で簡単に決められるアプリを作る。


- 学年ごとの傾斜を反映した計算が主目的である
- 対象学年は B3, B4, M1, M2 とする
- 学年ごとの金額は、可能な限り 500円単位の「きりの良い金額」に近づける
- 端数はそのまま隠さず、「きり良くするために生じた差額」として表示する
- 学年ごとの出力は一人当たりの負担額のみとする

## 優先度定義
- 1回目のリリース: 最低限の価値を持つ機能を実装する
- 2回目のリリース: 1回目の改善と使いやすさの向上を行う
- 3回目のリリース: 将来の拡張や運用性を高める機能を追加する

## 機能一覧

### 1回目のリリース
1. 学年ごとの人数入力
   - 学年を B3, B4, M1, M2 の4カテゴリで管理できる
   - 各学年ごとに人数を数値入力で設定できる

2. 合計金額の入力
   - 合計金額を数値入力で設定できる

3. 学年ごとの金額出力
   - 各学年ごとに一人当たりの負担額を表示する
   - 表示金額はなるべく 500円単位のきりの良い金額になるように調整する
   - もし実際の計算で端数が出た場合は、その差額を表示して理由を分かるようにする

### 2回目のリリース
4. 端数の表示
   - 傾斜を反映した計算で発生した端数を明示する
   - 端数は「きり良くするために生じた差額」として扱う
   - 例: 1人あたり 4,850 円になりそうだが、結果として 4,500 円 / 5,000 円 などの 500円単位で調整し、差額 500 円 を表示する

5. 複数の計算案を出力
   - 学年ごとの重みを複数用意し、それぞれの重みで異なる計算パターンを提示する
   - 例: 「均等寄り」「やや上級者寄り」「学年差を強めに反映」など
   - 重みごとに比較できるようにする

6. 目安金額の設定
   - 特定の学年に対して「目安の支払い額」を具体的な金額で設定できる
   - 例: M2 は 5,000円、B3 は 4,000円 など
   - 計算時にこの目安値を基準にして、他の学年との差分を補正する

### 3回目のリリース
7. 同額学年の考慮
   - 2つ以上の学年が同じ負担額になる場合も計算対象に含める
   - 例: B3とB4が同額、M1とM2が同額というようなグループ化を許容する

8. OB の追加
   - OB を対応できるように、学年カテゴリとして拡張可能にする

9. 結果の調整機能
   - 計算結果が出た後に、各学年の負担額を微調整できる
   - 例: 「M1を500円安く、B3を500円高く」などの修正が可能
   - 調整後の差分を可視化し、最終的な支払い額の整合性を確認できる

10. 過去の利用店と割り振り履歴の保存
    - どの店で飲んだか、どのように割り勘したかを履歴として残せる
    - 過去の支払い傾向や学年別の負担傾向を後から確認できる

## 初期リリースでの優先順位
1. 学年別人数入力
2. 合計金額入力
3. 学年ごとの負担額出力
4. 端数の表示
5. 重み付きの複数計算案の提示
6. 目安金額の設定

## 実装方針
- 最初は「人数 + 合計金額 + 学年ごとの目安金額」を入力して、すぐに結果が出る流れを重視する
- 計算は「基準額に対する傾斜補正」を基本モデルにし、複数案を比較できるようにする
- 均等割そのものは主機能にせず、傾斜のある割り勘を中心に設計する
- 金額はできるだけ 500円単位のきりの良い値に整え、残った端数は「調整差額」として表示する
- UIは最小限で操作しやすくし、P1以降の細かい調整機能を後から追加できるようにする
